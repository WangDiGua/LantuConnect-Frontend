/**
 * @deprecated Use buildPath / parseRoute from constants/consoleRoutes instead.
 * Kept for backward compatibility only.
 */
export { buildPath as buildConsolePath } from '../constants/consoleRoutes';
export { parseRoute as parseConsolePath } from '../constants/consoleRoutes';
